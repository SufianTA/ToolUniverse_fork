services:
  - type: web
    name: tooluniverse-smcp
    env: python
    plan: starter
    buildCommand: pip install -U pip && pip install .
    startCommand: tooluniverse-smcp-server --host 0.0.0.0 --port $PORT --rich-personas
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
